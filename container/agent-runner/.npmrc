# Prevent npm install from walking up to the root project's node_modules.
# Without this, installing here can blow away native addons (better-sqlite3)
# in the parent project.
install-strategy=nested
